name: 'zoom'
version: '1.0.0'
config-version: 2

profile: 'Analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target"]

models:
  zoom:
    bronze:
      +materialized: table
      +schema: bronze
      audit:
        +schema: bronze
        +materialized: table
        +tags: ["audit"]
